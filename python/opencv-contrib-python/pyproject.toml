[build-system]
requires = ["setuptools>=77"]
build-backend = "setuptools.build_meta"

[project]
name = "opencv-contrib-python"
version = "5.0.0.93"
description = "DrapeProof dependency shim selecting the headless OpenCV contrib wheel for server runtimes."
requires-python = ">=3.12"
dependencies = ["opencv-contrib-python-headless==5.0.0.93"]

[tool.setuptools]
py-modules = []
